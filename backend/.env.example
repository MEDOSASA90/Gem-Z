DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB_NAME"
JWT_SECRET="change_me_to_a_long_random_secret"
REFRESH_SECRET="change_me_to_another_long_random_secret"
NODE_ENV="production"

# Frontend / CORS
CLIENT_URL="https://your-frontend-domain.com"
API_URL="https://your-api-domain.com"

# Email (SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="your-email@gmail.com"
SMTP_PASS="your-app-password"
SMTP_FROM="Gem Z <noreply@gemz.app>"

# OpenAI
OPENAI_API_KEY="sk-..."

# Pi Network — https://developers.minepi.com
PI_API_KEY="your-pi-api-key-here"          # From Pi Developer Portal
PI_WALLET_SEED="your-pi-wallet-seed-here"   # From Pi Wallet (for A2U payments)

# Web Push (VAPID) — generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=""
VAPID_PRIVATE_KEY=""
VAPID_SUBJECT="mailto:your-email@gmail.com"
